#include <stdio.h>

int main()
{
    int i;
    for(i = 0; i < 10; i++)                 // 10 iterations
    {
        puts("SIGNAL COMPROMISED!\n");      // Prints
    }
    return 0;                               // Indicates the program ran successfully
}
